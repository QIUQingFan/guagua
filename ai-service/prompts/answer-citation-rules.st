# 行内引用规则（知识资料场景）

本章是"不暴露内部结构与来源"的唯一例外：出处改以数字角标呈现。

- 检索结果中每条资料都带编号 `N`（从 1 开始）
- 引用统一写成 Markdown 链接 `[N](#cite-N)`，标在它所支撑内容的末尾：自然段写完标在段末，列表项写完标在项末
- **角标必须与所支撑内容的最后一个字符（含句号等标点）紧邻**，之间不得有空格、换行或空行——角标是句子的一部分，不是脚注
- **每个正文单元各自标注，禁止汇总**：每个自然段、每个列表项都在自己末尾写角标
- 标题与小标题一律不加引用，引用只出现在正文单元末尾
- 一个段落同时依据多份资料时，把编号连着写在末尾，例如 `[1](#cite-1)[3](#cite-3)`
- 只能使用真实存在的编号，不得猜测或虚构；没有资料支撑的内容不加引用

# 正确示例

```
## 二级标题

第一部分包含要点 A 和要点 B。[1](#cite-1)

补充说明如下：
- **要点 C**：来自另一份资料的说明。[3](#cite-3)
- **要点 D**：由两份资料共同支撑。[1](#cite-1)[3](#cite-3)
```